@page "{id:int}"
@model enx_fit.Pages.Workouts.EditModel
@{
    ViewData["Title"] = "Edit workout";
}

<div class="mb-4">
    <h1 class="h3 mb-1">Edit workout</h1>
    <p class="text-muted mb-0">Update the session date, title, or notes.</p>
</div>

<div class="card">
    <div class="card-body">
        <form method="post">
            <input type="hidden" asp-for="Id" />
            @if (Model.IsAdministrator)
            {
                <div class="mb-3">
                    <label class="form-label" asp-for="Input.OwnerId"></label>
                    <select class="form-select" asp-for="Input.OwnerId">
                        <option value="">Выберите пользователя</option>
                        @foreach (var user in Model.Users)
                        {
                            <option value="@user.Id">@user.DisplayName</option>
                        }
                    </select>
                    <span class="text-danger" asp-validation-for="Input.OwnerId"></span>
                </div>
            }
            <partial name="_WorkoutForm" for="Input" />
            <div class="d-flex gap-2">
                <button class="btn btn-primary" type="submit">Save changes</button>
                <a class="btn btn-outline-secondary" asp-page="./Details" asp-route-id="@Model.Id">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
