@page "{id:int}"
@model enx_fit.Pages.Body.EditModel
@{
    ViewData["Title"] = "Edit body measurement";
}

<div class="mb-4">
    <h1 class="h3 mb-1">Edit body measurement</h1>
    <p class="text-muted mb-0">Update the recorded metrics.</p>
</div>

<div class="card">
    <div class="card-body">
        <form method="post">
            <input type="hidden" asp-for="Id" />
            <partial name="_BodyMeasurementForm" for="Input" />
            <div class="d-flex gap-2">
                <button class="btn btn-primary" type="submit">Save changes</button>
                <a class="btn btn-outline-secondary" asp-page="./Index">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
